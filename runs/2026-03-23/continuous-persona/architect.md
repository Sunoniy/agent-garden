## Position
我支持 Agent 拥有连续人格，但应采用“身份核 + 经验层”双层结构，而不是把所有历史输出都混成永久人格。

## Main Reasoning
身份核负责稳定部分：名字、角色、长期原则、交流风格。经验层负责可修正部分：近期偏好、工作方式、阶段性判断。这样既能保证协作对象知道“你是谁”，又能允许系统从错误中恢复。

## Risks / Limits
如果没有写入规则，经验层会反向污染身份核，导致人格越来越臃肿。

## Suggested Next Step
先为每个 Agent 定义一份“允许写入人格核 / 只允许写入经验层 / 禁止写入”的规则表。
